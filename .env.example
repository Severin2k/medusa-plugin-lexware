# Verschluesselungsschluessel fuer API Key und SMTP-Passwort in der Datenbank
# 32 Bytes als Hex-String generieren:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
LEXWARE_ENCRYPTION_KEY=
